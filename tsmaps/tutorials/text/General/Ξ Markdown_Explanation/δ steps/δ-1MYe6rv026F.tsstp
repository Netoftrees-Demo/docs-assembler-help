# What is Markdown?

Markdown is a **plain-text formatting syntax** designed to be easy to read in its raw form and easy to convert into clean HTML. Think of it as a middle ground between writing a plain email and coding a webpage.

If you stripped away the renderer, a Markdown document should still make perfect sense as plain text — unlike raw HTML, which is cluttered with angle brackets and closing tags.

